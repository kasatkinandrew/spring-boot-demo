logging:
  level:
    org.springframework.web: debug

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/testdb
    username: root
    password: 1111

  jpa:
    hibernate:
      ddl-auto: update
